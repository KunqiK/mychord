# Local test/session artifacts — not part of the project
*.webm
*.log
image.png
.playwright-mcp/

# Separate nested git repo (own project, not tracked here)
Music-Source-Separation-Training-GUI/
MIDIandMUSIC/audio/
omr/venv/

# Fine-tune artifacts (models/features are rebuildable; keep code + split only)
btc_finetune/*.pt
btc_finetune/features/
MIDIandMUSIC/dataset/
__pycache__/
